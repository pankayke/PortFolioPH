// lib/features/recruiter/screens/jobs/job_create_screen.dart
// lib/features/recruiter/screens/jobs/job_list_screen.dart
// lib/features/recruiter/screens/applicants/applicants_screen.dart
// lib/features/seeker/screens/jobs/job_list_screen.dart
// lib/features/seeker/screens/profile/seeker_profile_screen.dart

// PLACEHOLDER SCREENS - Implement these based on requirements

import 'package:flutter/material.dart';

// Job Create Screen
class JobCreateScreen extends StatelessWidget {
  const JobCreateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Job')),
      body: const Center(child: Text('Job Create - Coming Soon')),
    );
  }
}

// Recruiter Jobs List Screen
class JobListScreen extends StatelessWidget {
  const JobListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Jobs')),
      body: const Center(child: Text('Jobs List - Coming Soon')),
    );
  }
}

// Applicants Screen
class ApplicantsScreen extends StatelessWidget {
  const ApplicantsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Applicants')),
      body: const Center(child: Text('Applicants - Coming Soon')),
    );
  }
}

// Seeker Jobs List Screen
class SeekerJobListScreen extends StatelessWidget {
  const SeekerJobListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Browse Jobs')),
      body: const Center(child: Text('Browse Jobs - Coming Soon')),
    );
  }
}

// Seeker Profile Screen
class SeekerProfileScreen extends StatelessWidget {
  const SeekerProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Profile')),
      body: const Center(child: Text('Profile - Coming Soon')),
    );
  }
}
